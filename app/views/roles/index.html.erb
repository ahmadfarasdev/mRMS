<div class="module-title">
  <h2 class='module-title-text'>ROLES</h2>
</div>

<section id="widget-grid" class="roles_section">
    <div class="row" style="margin-bottom: 10px">
      <div class="col-md-12 ">
        <div class="pull-right">
          <% if can?(:manage_roles) %>
              <%= show_button(new_role_path, 'New Role', 'plus', 'primary') %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="row">
      <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <%= render partial: 'list',
                   layout: 'shared/smart_widget_wrapper',
                   locals: {
                       wid_id: 'roles',
                       wid_title: ' '
                   } %>
      </article>
    </div>
  </section>
